Basic Express Site
==================

Build
-----

Run this command in console:

```
npm install
```
Run
---

Run this command in console:

```
node app.js
```

Open `http://localhost:3000` to access basic Express Site.
